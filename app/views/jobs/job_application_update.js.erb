<% if request.post? %>
  <% if @job_application.errors.any? %>
  $("#comment_error").append("<%= escape_javascript("comment should not be to short and fields should not be empty!") %>");
   <% else %>
		location.reload();
	<% end %>
<% else %>
	$("#job_status").html("<%= escape_javascript(render 'jobs/job_status_modal') %>");
	$("#job_status").modal();
<% end %>




