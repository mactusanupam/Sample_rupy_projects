$("#preview-cv").html("<%= escape_javascript(render 'digital_cvs/preview') %>");
$("#preview-cv").modal();

$('#online-resume').popover({
  html : true,
  placement: 'bottom',
  content: function() {
    return $('#preview-cv #popover-content-wrapper').html();
  }
});
