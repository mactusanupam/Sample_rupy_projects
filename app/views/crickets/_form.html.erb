<%= form_with(model: cricket, local: true) do |form| %>
  <% if cricket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cricket.errors.count, "error") %> prohibited this cricket from being saved:</h2>

      <ul>
      <% cricket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team %>
    <%= form.text_field :team, id: :cricket_team %>
  </div>

  <div class="field">
    <%= form.label :palyer %>
    <%= form.text_field :palyer, id: :cricket_palyer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
